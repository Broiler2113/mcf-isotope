# RL Enemy AI v1 trainer (spec Section 1, C1) + the Section 11 dashboard. CPU torch is
# pinned via --index-url in rl/setup.sh; on macOS the same wheel carries MPS.
torch>=2.4
numpy>=1.26
tensorboard>=2.16
pyyaml>=6.0
onnx>=1.16
onnxruntime>=1.18
streamlit>=1.40
pandas>=2.2
