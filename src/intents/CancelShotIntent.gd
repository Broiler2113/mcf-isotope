class_name CancelShotIntent
extends Intent

## Снять незавершённую очередь (#12). ОД не возвращает: действие «Стрельба» уже
## оплачено, отменяются лишь недострелянные пули. Отдельным намерением, а не правкой
## action_state из UI, потому что state меняет ТОЛЬКО резолвер — иначе клиент об
## отмене не узнает и досчитает очередь, которой у хоста уже нет.

func _init(p_actor_id: int) -> void:
	super(p_actor_id)
